import Card from "../common/Card";

// PUBLIC_INTERFACE
export default function BreathingCoach() {
  /** Breathing coach stub. */
  return (
    <Card title="Breathing Coach">
      <div>Inhale 4s, hold 4s, exhale 4s.</div>
    </Card>
  );
}
