import Card from "../common/Card";

// PUBLIC_INTERFACE
export default function HabitBuilder() {
  /** Habit builder stub. */
  return (
    <Card title="Habit Builder">
      <div>Create a small habit to reinforce goals.</div>
    </Card>
  );
}
